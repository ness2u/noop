reviewer: git-2d (the TPM)
sha: 99f0c23
blind: partial — written from the diff before reading any pack row; the pack's one-word headline for this sha was named in the request that asked for the review
verdict: findings 3
- floating_tag noop-service — (k8s/deployment.yaml) `registry.nessh:30500/loch-nessh/noop:master_latest` with `imagePullPolicy: Always`; the change swapped the registry name and kept the float, so every restart adopts whatever was pushed last; the comment defers it to the gate's RC tag
- other observe.go — the `path` label is `r.URL.Path` unbounded, and `/` is a catch-all handler, so every distinct URL a client invents creates a new requests series and a new duration histogram; a scanner grows the metrics maps without limit (a cardinality leak that is not the chaos one and does not read as one)
- other Dockerfile — two defaults for "not stamped": the image says `unstamped`, a local build says `dev`; `/version` and `noop_build_info` cannot say "built without a stamp" with one value
notes:
- `/crash` panics inside a goroutine, which the middleware's `recover` cannot catch, so the process still dies: right for a chaos target; the recovered-panic path covers a handler panic only
- probes present, both on `/healthz`, readiness every 5 s with no initial delay; the scrape annotations name port and path
- no tests, stated on purpose in the message for the gate's first refusal
